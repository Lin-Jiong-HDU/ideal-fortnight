# 深色模式支持实现计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为 GEO 优化平台添加完整的深色模式支持，包括主题切换按钮和平滑过渡效果

**Architecture:** 使用 next-themes 管理主题状态，通过 CSS 变量定义颜色系统，Tailwind 的 dark: 变体处理样式切换

**Tech Stack:** next-themes, Tailwind CSS v4, React 19, Next.js 16.2

---

## Chunk 1: 基础设施搭建

### Task 1: 安装依赖

- [ ] **Step 1: 安装 next-themes**

```bash
pnpm add next-themes
```

Expected: package 安装成功

---

### Task 2: 创建 ThemeProvider

**Files:**
- Create: `components/providers/theme-provider.tsx`

- [ ] **Step 1: 创建 ThemeProvider 组件**

```tsx
'use client';

import { ThemeProvider as NextThemesProvider } from 'next-themes';
import { type ThemeProviderProps } from 'next-themes/dist/types';

export function ThemeProvider({ children, ...props }: ThemeProviderProps) {
  return <NextThemesProvider {...props}>{children}</NextThemesProvider>;
}
```

- [ ] **Step 2: 在根布局中集成 ThemeProvider**

**Files:**
- Modify: `app/layout.tsx`

读取现有的 `app/layout.tsx`，用 `ThemeProvider` 包裹 `{children}`：

```tsx
import { ThemeProvider } from '@/components/providers/theme-provider';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN" suppressHydrationWarning>
      <body>
        <ThemeProvider
          attribute="class"
          defaultTheme="system"
          enableSystem
          disableTransitionOnChange
        >
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
```

注意：`suppressHydrationWarning` 用于防止 next-themes 在服务端和客户端 HTML 不一致时产生警告

- [ ] **Step 3: 提交**

```bash
git add components/providers/theme-provider.tsx app/layout.tsx
git commit -m "feat: add ThemeProvider for dark mode support"
```

---

### Task 3: 更新 CSS 变量系统

**Files:**
- Modify: `app/globals.css`

- [ ] **Step 1: 替换现有的 CSS 变量定义**

完全替换 `app/globals.css` 为：

```css
@import "tailwindcss";

:root {
  /* 基础颜色 - 浅色模式 */
  --background: 0 0% 100%;
  --foreground: 0 0% 3.9%;

  /* 组件背景 */
  --card: 0 0% 100%;
  --card-foreground: 0 0% 3.9%;

  /* 次要/强调 */
  --muted: 0 0% 96.1%;
  --muted-foreground: 0 0% 45.1%;

  /* 边框和分割线 */
  --border: 0 0% 89.8%;
  --input: 0 0% 89.8%;
  --ring: 0 0% 3.9%;

  /* 主色调 */
  --primary: 221.2 83.2% 53.3%;
  --primary-foreground: 0 0% 98%;

  /* 其他状态色 */
  --secondary: 0 0% 96.1%;
  --destructive: 0 84.2% 60.2%;
  --destructive-foreground: 0 0% 98%;

  --radius: 0.5rem;
}

.dark {
  /* 深色模式 */
  --background: 0 0% 3.9%;
  --foreground: 0 0% 98%;

  --card: 0 0% 3.9%;
  --card-foreground: 0 0% 98%;

  --muted: 0 0% 14.9%;
  --muted-foreground: 0 0% 63.9%;

  --border: 0 0% 14.9%;
  --input: 0 0% 14.9%;
  --ring: 0 0% 83.1%;

  --primary: 217.2 91.2% 59.8%;
  --primary-foreground: 0 0% 98%;

  --secondary: 0 0% 14.9%;
  --destructive: 0 62.8% 30.6%;
  --destructive-foreground: 0 0% 98%;
}

@theme inline {
  --color-background: hsl(var(--background));
  --color-foreground: hsl(var(--foreground));
  --color-card: hsl(var(--card));
  --color-card-foreground: hsl(var(--card-foreground));
  --color-muted: hsl(var(--muted));
  --color-muted-foreground: hsl(var(--muted-foreground));
  --color-border: hsl(var(--border));
  --color-input: hsl(var(--input));
  --color-ring: hsl(var(--ring));
  --color-primary: hsl(var(--primary));
  --color-primary-foreground: hsl(var(--primary-foreground));
  --color-secondary: hsl(var(--secondary));
  --color-destructive: hsl(var(--destructive));
  --color-destructive-foreground: hsl(var(--destructive-foreground));
  --font-sans: var(--font-geist-sans);
  --font-mono: var(--font-geist-mono);
}

body {
  background: hsl(var(--background));
  color: hsl(var(--foreground));
  font-family: Arial, Helvetica, sans-serif;
}

/* 平滑过渡效果 */
* {
  transition-property: background-color, border-color, color, fill, stroke;
  transition-timing-function: cubic-bezier(0.4, 0, 0.2, 1);
  transition-duration: 150ms;
}

/* line-clamp 工具类 */
.line-clamp-2 {
  overflow: hidden;
  display: -webkit-box;
  -webkit-box-orient: vertical;
  -webkit-line-clamp: 2;
}
```

- [ ] **Step 2: 提交**

```bash
git add app/globals.css
git commit -m "feat: add comprehensive CSS variable system for dark mode"
```

---

## Chunk 2: 主题切换组件

### Task 4: 创建 ThemeToggle 组件

**Files:**
- Create: `components/theme/theme-toggle.tsx`

- [ ] **Step 1: 创建主题切换组件**

```tsx
'use client';

import { useTheme } from 'next-themes';
import { useEffect, useState } from 'react';
import { Menu, MenuButton, MenuItem, MenuPopover, MenuTrigger } from '@base-ui/react/react';

export function ThemeToggle() {
  const { theme, setTheme } = useTheme();
  const [mounted, setMounted] = useState(false);

  // 避免服务端渲染不匹配
  useEffect(() => {
    setMounted(true);
  }, []);

  if (!mounted) {
    return (
      <button className="w-full px-4 py-2 text-sm text-gray-700 rounded-lg opacity-50">
        加载中...
      </button>
    );
  }

  const themes = [
    { value: 'light', label: '浅色', icon: '☀️' },
    { value: 'dark', label: '深色', icon: '🌙' },
    { value: 'system', label: '跟随系统', icon: '💻' },
  ];

  const currentTheme = themes.find(t => t.value === theme) || themes[2];

  return (
    <Menu>
      <MenuTrigger>
        <MenuButton className="w-full px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800 rounded-lg transition-colors flex items-center gap-2">
          <span>{currentTheme.icon}</span>
          <span className="flex-1 text-left">{currentTheme.label}</span>
        </MenuButton>
      </MenuTrigger>
      <MenuPopover className="z-50 min-w-[150px] bg-white dark:bg-gray-900 border border-gray-200 dark:border-gray-700 rounded-lg shadow-lg p-1">
        <Menu>
          {themes.map((t) => (
            <MenuItem
              key={t.value}
              onClick={() => setTheme(t.value)}
              className={`
                px-3 py-2 text-sm rounded-md cursor-pointer
                ${theme === t.value
                  ? 'bg-blue-50 dark:bg-blue-900/30 text-blue-600 dark:text-blue-400 font-medium'
                  : 'text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-gray-800'
                }
              `}
            >
              <span className="flex items-center gap-2">
                <span>{t.icon}</span>
                <span>{t.label}</span>
              </span>
            </MenuItem>
          ))}
        </Menu>
      </MenuPopover>
    </Menu>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add components/theme/theme-toggle.tsx
git commit -m "feat: add ThemeToggle component with menu"
```

---

## Chunk 3: 集成到布局

### Task 5: 在 Sidebar 中添加主题切换按钮

**Files:**
- Modify: `components/layout/sidebar.tsx`

- [ ] **Step 1: 在用户区域添加 ThemeToggle**

找到侧边栏底部的 User Info 区域（大约在 147 行开始），在退出登录按钮之前插入 ThemeToggle：

```tsx
import { ThemeToggle } from '@/components/theme/theme-toggle';

// ...

// User Info 部分修改为：
<div className="p-4 border-t border-gray-200">
  <div className="flex items-center gap-3 mb-3">
    {/* ... 现有的用户头像和信息 ... */}
  </div>
  <ThemeToggle />
  <button
    onClick={logout}
    className="w-full px-4 py-2 text-sm text-gray-700 hover:bg-gray-100 rounded-lg transition-colors"
  >
    退出登录
  </button>
</div>
```

同时更新整个 sidebar 的硬编码颜色，替换为语义化类名。

- [ ] **Step 2: 完整更新 Sidebar 颜色类名**

替换整个 `components/layout/sidebar.tsx` 为（保留原有逻辑，只修改类名）：

```tsx
'use client';

import { useState } from 'react';
import Link from 'next/link';
import { usePathname, useRouter } from 'next/navigation';
import { useAuth } from '@/lib/contexts/AuthContext';
import { cn } from '@/lib/utils';
import { ThemeToggle } from '@/components/theme/theme-toggle';

interface NavItem {
  label: string;
  href: string;
  icon: string;
  subitems?: NavItem[];
}

const navItemsByRole: Record<string, NavItem[]> = {
  admin: [
    { label: '工作台', href: '/dashboard', icon: '📊' },
    { label: '工单池', href: '/optimizer/tickets', icon: '📥' },
    { label: '我的工单', href: '/tickets', icon: '📋' },
    { label: '创建工单', href: '/tickets/new', icon: '➕' },
    {
      label: '管理中心',
      href: '/admin',
      icon: '⚙️',
      subitems: [
        { label: '用户管理', href: '/admin/users', icon: '👥' },
        { label: '客户管理', href: '/admin/customers', icon: '🏢' },
        { label: '配额管理', href: '/admin/quotas', icon: '💎' },
        { label: '项目管理', href: '/admin/projects', icon: '📁' },
      ],
    },
  ],
  optimizer: [
    { label: '工作台', href: '/dashboard', icon: '📊' },
    { label: '工单池', href: '/optimizer/tickets', icon: '📥' },
    { label: '我的工单', href: '/tickets', icon: '📋' },
    { label: '创建工单', href: '/tickets/new', icon: '➕' },
    {
      label: '管理中心',
      href: '/admin',
      icon: '⚙️',
      subitems: [
        { label: '客户列表', href: '/admin/customers', icon: '🏢' },
        { label: '项目列表', href: '/admin/projects', icon: '📁' },
      ],
    },
  ],
  customer: [
    { label: '工作台', href: '/dashboard', icon: '📊' },
    { label: '我的工单', href: '/tickets', icon: '📋' },
    { label: '提交工单', href: '/tickets/new', icon: '➕' },
  ],
};

export function Sidebar() {
  const pathname = usePathname();
  const router = useRouter();
  const { user, logout } = useAuth();
  const [expandedMenus, setExpandedMenus] = useState<Set<string>>(new Set());

  if (!user) return null;

  const navItems = navItemsByRole[user.role] || [];

  const toggleMenu = (href: string) => {
    setExpandedMenus((prev) => {
      const newSet = new Set(prev);
      if (newSet.has(href)) {
        newSet.delete(href);
      } else {
        newSet.add(href);
      }
      return newSet;
    });
  };

  const isItemActive = (item: NavItem): boolean => {
    if (pathname === item.href || pathname.startsWith(item.href + '/')) {
      return true;
    }
    if (item.subitems) {
      return item.subitems.some((subitem) => pathname === subitem.href || pathname.startsWith(subitem.href + '/'));
    }
    return false;
  };

  const renderNavItem = (item: NavItem, depth = 0) => {
    const isActive = isItemActive(item);
    const isExpanded = expandedMenus.has(item.href);
    const hasSubitems = item.subitems && item.subitems.length > 0;

    return (
      <li key={item.href}>
        <div
          className={cn(
            'flex items-center gap-3 px-4 py-3 rounded-lg transition-colors cursor-pointer',
            isActive
              ? 'bg-primary/10 text-primary font-medium'
              : 'text-foreground hover:bg-muted',
            depth > 0 && 'ml-4'
          )}
          onClick={() => {
            if (hasSubitems) {
              toggleMenu(item.href);
            } else {
              router.push(item.href);
            }
          }}
        >
          <span className="text-lg">{item.icon}</span>
          <span className="flex-1">{item.label}</span>
          {hasSubitems && (
            <span
              className={cn(
                'transition-transform',
                isExpanded ? 'rotate-90' : ''
              )}
            >
              ▶
            </span>
          )}
        </div>
        {hasSubitems && isExpanded && (
          <ul className="mt-1 space-y-1">
            {item.subitems!.map((subitem) => renderNavItem(subitem, depth + 1))}
          </ul>
        )}
      </li>
    );
  };

  return (
    <aside className="w-64 bg-card border-r border-border flex flex-col h-screen sticky top-0">
      {/* Logo */}
      <div className="p-6 border-b border-border">
        <h1 className="text-xl font-bold bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
          GEO 优化平台
        </h1>
      </div>

      {/* Navigation */}
      <nav className="flex-1 p-4">
        <ul className="space-y-2">{navItems.map((item) => renderNavItem(item))}</ul>
      </nav>

      {/* User Info */}
      <div className="p-4 border-t border-border">
        <div className="flex items-center gap-3 mb-3">
          <div className="w-10 h-10 rounded-full bg-gradient-to-br from-blue-500 to-purple-500 flex items-center justify-center text-white font-medium">
            {user.name.charAt(0).toUpperCase()}
          </div>
          <div className="flex-1 min-w-0">
            <p className="text-sm font-medium text-foreground truncate">{user.name}</p>
            <p className="text-xs text-muted-foreground truncate">{user.email}</p>
          </div>
        </div>
        <ThemeToggle />
        <button
          onClick={logout}
          className="w-full px-4 py-2 text-sm text-foreground hover:bg-muted rounded-lg transition-colors"
        >
          退出登录
        </button>
      </div>
    </aside>
  );
}
```

- [ ] **Step 3: 提交**

```bash
git add components/layout/sidebar.tsx
git commit -m "feat: integrate theme toggle into sidebar"
```

---

## Chunk 4: 更新业务组件颜色

### Task 6: 更新 Dashboard 布局颜色

**Files:**
- Modify: `app/(dashboard)/layout.tsx`

- [ ] **Step 1: 替换硬编码颜色类名**

```tsx
'use client';

import { useEffect } from 'react';
import { useRouter } from 'next/navigation';
import { useAuth } from '@/lib/contexts/AuthContext';
import { Sidebar } from '@/components/layout/sidebar';

export default function DashboardLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  const router = useRouter();
  const { isAuthenticated, isLoading } = useAuth();

  useEffect(() => {
    if (!isLoading && !isAuthenticated) {
      router.push('/login');
    }
  }, [isAuthenticated, isLoading, router]);

  if (isLoading) {
    return (
      <div className="min-h-screen flex items-center justify-center">
        <div className="text-muted-foreground">加载中...</div>
      </div>
    );
  }

  if (!isAuthenticated) {
    return null;
  }

  return (
    <div className="flex min-h-screen bg-muted">
      <Sidebar />
      <main className="flex-1">
        {children}
      </main>
    </div>
  );
}
```

- [ ] **Step 2: 提交**

```bash
git add app/\(dashboard\)/layout.tsx
git commit -m "refactor: update dashboard layout colors for dark mode"
```

---

### Task 7: 更新其他页面组件

需要检查并更新以下页面中的硬编码颜色：

**Files to check:**
- `app/(auth)/login/page.tsx`
- `app/(dashboard)/tickets/page.tsx`
- `app/(dashboard)/tickets/new/page.tsx`
- `app/(dashboard)/optimizer/tickets/page.tsx`
- `app/(dashboard)/tickets/[id]/page.tsx`
- `app/(dashboard)/admin/*/page.tsx` (所有 admin 页面)

- [ ] **Step 1: 阅读每个文件，识别硬编码颜色**

使用 `Grep` 查找包含硬编码颜色的文件：

```bash
grep -r "bg-gray-\|text-gray-\|border-gray-" app/\(dashboard\) components/ --include="*.tsx" | head -50
```

- [ ] **Step 2: 逐个文件替换颜色类名**

根据搜索结果，对每个文件进行替换：

| 查找 | 替换为 |
|------|--------|
| `text-gray-500` | `text-muted-foreground` |
| `text-gray-700` | `text-foreground` |
| `text-gray-900` | `text-foreground` |
| `bg-gray-50` | `bg-muted` |
| `bg-gray-100` | `bg-muted` (hover 状态) |
| `border-gray-200` | `border-border` |

- [ ] **Step 3: 提交**

```bash
git add app/\(dashboard\) components/
git commit -m "refactor: replace hardcoded colors with semantic classes"
```

---

## Chunk 5: 测试和验证

### Task 8: 手动测试验证

- [ ] **Step 1: 启动开发服务器**

```bash
pnpm dev
```

- [ ] **Step 2: 测试主题切换功能**

1. 打开浏览器访问 http://localhost:3000
2. 登录后，点击侧边栏底部的主题切换按钮
3. 依次测试浅色、深色、跟随系统三个选项
4. 刷新页面，验证主题选择被保持
5. 检查所有页面在深色模式下的显示效果

- [ ] **Step 3: 检查对比度问题**

在深色模式下，检查以下元素是否有对比度问题：
- 文本是否清晰可读
- 边框是否可见
- 表单输入框是否清晰
- 卡片背景与主背景是否有区分

- [ ] **Step 4: 测试系统主题跟随**

1. 在操作系统中切换深色/浅色模式
2. 选择"跟随系统"选项
3. 验证网站主题是否跟随系统变化

---

### Task 9: 最终检查

- [ ] **Step 1: 检查所有页面**

访问所有页面，确保没有遗漏的硬编码颜色：
- /dashboard
- /tickets
- /tickets/new
- /optimizer/tickets
- /admin/users
- /admin/customers
- /admin/projects
- /admin/quotas

- [ ] **Step 2: 检查控制台错误**

打开浏览器开发者工具，检查是否有任何错误或警告

- [ ] **Step 3: 验证 TypeScript 编译**

```bash
pnpm build
```

确保没有 TypeScript 错误

- [ ] **Step 4: 最终提交**

```bash
git add .
git commit -m "chore: final dark mode implementation complete"
```

---

## 附录：颜色类名映射表

| 原类名 (浅色) | 新类名 (语义化) |
|--------------|----------------|
| `bg-white` | `bg-card` |
| `bg-gray-50` | `bg-muted` |
| `bg-gray-100` | `bg-muted` |
| `text-gray-500` | `text-muted-foreground` |
| `text-gray-700` | `text-foreground` |
| `text-gray-900` | `text-foreground` |
| `border-gray-200` | `border-border` |
| `border-gray-300` | `border-border` |
| `hover:bg-gray-100` | `hover:bg-muted` |
| `focus:ring-gray-200` | `focus:ring-border` |

特殊处理：
- `bg-blue-50 text-blue-600` (选中状态) → `bg-primary/10 text-primary`
- 渐变色 (如 `bg-gradient-to-r`) 可以保持不变
